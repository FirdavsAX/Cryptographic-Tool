<UserControl x:Class="Cryptographic_Tool.Views.SymmetricEncryptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="#121212">

    <UserControl.Resources>
        <!-- Dark Theme Colors matching MainWindow -->
        <SolidColorBrush x:Key="DarkBackground" Color="#121212"/>
        <SolidColorBrush x:Key="DarkSurface" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="DarkElement" Color="#2C2C2C"/>
        <SolidColorBrush x:Key="LightText" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF4CAF50"/>
        <SolidColorBrush x:Key="AccentTeal" Color="#FF009688"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#FFFF9800"/>
        <SolidColorBrush x:Key="BorderBrushColor" Color="#383838"/>
        <SolidColorBrush x:Key="MutedText" Color="#888888"/>
        <SolidColorBrush x:Key="AlternateRowBackground" Color="#252525"/>

        <!-- Style for labels -->
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Foreground" Value="{StaticResource LightText}"/>
        </Style>

        <!-- Style for TextBoxes with dark theme -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource DarkElement}"/>
            <Setter Property="Foreground" Value="{StaticResource LightText}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource LightText}"/>
        </Style>

        <!-- Style for Action Buttons -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#0066FF"/>
            <Setter Property="BorderBrush" Value="#0052CC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="6"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1a75ff"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0047b3"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0047b3"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="#e6e6e6"/>
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#cccccc"/>
                                <Setter Property="Foreground" Value="#777"/>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RadioButton Style for Dark Theme -->
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource LightText}"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <!-- Placeholder-like TextBlock attached to TextBoxes -->
        <Style TargetType="TextBox" x:Key="PlaceholderTextBoxStyle" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Placeholder text, visible when Text is empty -->
                            <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                       Foreground="#888888"
                                       Margin="{TemplateBinding Padding}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TextBox}}}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <!-- The actual input area -->
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Accent Brushes (Hashing, Action - Only those strictly needed here) -->
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#FFFF9800" />
        <SolidColorBrush x:Key="AccentTealBrush" Color="#FF009688" />


        <!-- DataGrid Style for Dark Theme -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource DarkSurface}"/>
            <Setter Property="Foreground" Value="{StaticResource LightText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="{StaticResource DarkSurface}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource AlternateRowBackground}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrushColor}"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource DarkElement}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header & Algorithm -->
            <RowDefinition Height="Auto"/>
            <!-- Input, Key, IV -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons & Output -->
            <RowDefinition Height="*"/>
            <!-- Results Table -->
        </Grid.RowDefinitions>

        <!-- ============================================== -->
        <!-- HEADER AND ALGORITHM SELECTION (Row 0) -->
        <!-- ============================================== -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Symmetric Encryption"
                       FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentGreen}" Margin="0,0,0,10"/>

            <Border Padding="15" Background="{StaticResource DarkSurface}" CornerRadius="4" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Select Algorithm:" Style="{StaticResource LabelStyle}" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding Algorithms}" SelectedItem="{Binding SelectedAlgorithm, Mode=TwoWay}" Width="220" Background="{StaticResource DarkElement}" Foreground="{StaticResource LightText}"/>
                </StackPanel>
            </Border>
        </StackPanel>


        <!-- ============================================== -->
        <!-- INPUTS (Row 1) -->
        <!-- Using a Grid for flexible alignment of Key/IV beside Input Text -->
        <!-- ============================================== -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <!-- Input Text (Wider) -->
                <ColumnDefinition Width="15"/>
                <!-- Spacer -->
                <ColumnDefinition Width="*"/>
                <!-- Key/IV (Narrower) -->
            </Grid.ColumnDefinitions>

            <!-- Input Text -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Input Data (Plaintext or Ciphertext)" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding PlainText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Top"
                         Height="120" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Tag="Paste data here..."
                         Style="{StaticResource PlaceholderTextBoxStyle}"/>
            </StackPanel>

            <!-- Key and IV Inputs -->
            <StackPanel Grid.Column="2">
                <!-- Key Input -->
                <TextBlock Text="Custom Key (Optional)" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,15"
                         Tag="Enter key (Base64)..."
                         Style="{StaticResource PlaceholderTextBoxStyle}"/>

                <!-- IV Input -->
                <TextBlock Text="Custom IV (Optional)" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Iv, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Enter IV (Base64 or Hex)..."
                         Style="{StaticResource PlaceholderTextBoxStyle}"/>
            </StackPanel>
        </Grid>


        <!-- ============================================== -->
        <!-- ACTIONS AND OUTPUT (Row 2) -->
        <!-- ============================================== -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <!-- Buttons & Busy Indicator -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Encrypt" Command="{Binding EncryptCommand}" Grid.Column="0" Background="{StaticResource AccentGreen}"/>
                <Button Content="Decrypt" Command="{Binding DecryptCommand}" Grid.Column="1" Background="{StaticResource AccentTeal}" Margin="10,0,0,0"/>

            </Grid>

            <!-- Output Text -->
            <TextBlock Text="Output Result" Style="{StaticResource LabelStyle}"/>
            <!-- bind to OutputText so decrypt result shows here -->
            <TextBox Text="{Binding OutputText, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Height="120"
                     Background="{StaticResource DarkElement}"
                     Tag="Result will appear here..."
                     Style="{StaticResource PlaceholderTextBoxStyle}"/>
        </StackPanel>


        <!-- ============================================== -->
        <!-- RESULTS HISTORY DATAGRID (Row 3) -->
        <!-- ============================================== -->
        <Border Grid.Row="3" Padding="15" Background="{StaticResource DarkSurface}" CornerRadius="4" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Encryption History Performance" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="{StaticResource AccentGreen}"/>
                <DataGrid ItemsSource="{Binding Results}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Algorithm" Binding="{Binding Algorithm}" Width="150"/>
                        <DataGridTextColumn Header="Operation" Binding="{Binding Operation}" Width="120"/>
                        <DataGridTextColumn Header="Time (ms)" Binding="{Binding TimeMs, StringFormat={}{0:F2}}" Width="120"/>
                        <DataGridTextColumn Header="Result (Preview)" Binding="{Binding PreviewText}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <!-- Overlay Loader: covers entire area, visible when ViewModel.IsBusy is true -->
        <Grid Grid.Row="0" Grid.RowSpan="4"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
              Panel.ZIndex="100"
              IsHitTestVisible="{Binding IsBusy}">
            <!-- semi-transparent dim -->
            <Border Background="#80000000" />
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical" >
                <!-- Indeterminate progress bar styled for dark theme -->
                <Border Background="{StaticResource DarkSurface}" Padding="12" CornerRadius="8" Opacity="0.95">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar Width="260" Height="10" IsIndeterminate="True" Foreground="{StaticResource AccentTealBrush}" />
                        <TextBlock Text="Loading..." Foreground="{StaticResource LightText}" FontFamily="Segoe UI" FontWeight="SemiBold"
                                   Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>